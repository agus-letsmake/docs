---
title: CLI & SDK overview
description: "Setup-only CLI plus Client and Playwright packages"
---

| Package                             | Role                                                   |
| ----------------------------------- | ------------------------------------------------------ |
| `@ovenbits/make-testlab-playwright` | Playwright adapter + `testlab` CLI (`login` / `init`)  |
| `@ovenbits/make-testlab-client`     | Shared reporting brain (start/push/complete, evidence) |

Install from GitHub Packages. TestLab login does **not** authenticate npm —
export `NODE_AUTH_TOKEN` with `read:packages` (SSO-authorize the org). See
[Playwright SDK → Install](/docs/cli-sdk/playwright#install).

```bash
npm install @ovenbits/make-testlab-playwright @ovenbits/make-testlab-client
```

<Warning>
  Do **not** use the retired name `make-testlab-playwright-reporter`.
</Warning>

## Path

1. [CLI login + init](/docs/cli-sdk/cli-login-init)
2. Import catalog in the [Cases UI](/docs/web-app/cases)
3. [Playwright reporter](/docs/cli-sdk/playwright) for day-to-day runs
4. Or call the [Client](/docs/cli-sdk/client) from a non-Playwright runner

## What the Playwright path covers

| Concern                    | Where documented                                                                                      |
| -------------------------- | ----------------------------------------------------------------------------------------------------- |
| Send results               | [Playwright SDK → How results are sent](/docs/cli-sdk/playwright#how-results-are-sent)                |
| Attach screenshots         | [Playwright SDK → Screenshots and videos](/docs/cli-sdk/playwright#screenshots-and-videos)            |
| Test steps / Observed      | [Playwright SDK → Test steps](/docs/cli-sdk/playwright#test-steps-observed-evidence)                  |
| Evidence policy            | [Evidence](/docs/concepts/evidence)                                                                   |
| Env vars                   | [Reporting env](/docs/concepts/reporting-env)                                                         |
| Catalog / suite membership | [Portable Catalog](/docs/concepts/portable-catalog)                                                   |
| Non-Playwright runners     | [Client → Non-Playwright](/docs/cli-sdk/client#non-playwright-runners-maestro-junit-custom)           |
| CI/CD                      | [GitHub Actions](/docs/ci-automations/github-actions) + [CI/CD guide](/docs/guides/ci-cd-integration) |
| Flutter web sample         | [agus-letsmake/flutter-web-playwright](https://github.com/agus-letsmake/flutter-web-playwright)       |
