---
title: CLI & SDK overview
description: "Setup-only CLI plus Client and Playwright packages"
---

| Package                             | Role                                                   |
| ----------------------------------- | ------------------------------------------------------ |
| `@ovenbits/make-testlab-playwright` | Playwright adapter + `testlab` CLI (`login` / `init`)  |
| `@ovenbits/make-testlab-client`     | Shared reporting brain (start/push/complete, evidence) |

Install from GitHub Packages. TestLab login does **not** authenticate npm —
export `NODE_AUTH_TOKEN` with `read:packages` (SSO-authorize the org). See
[Playwright SDK → Install](/cli-sdk/playwright#install).

```bash
npm install @ovenbits/make-testlab-playwright @ovenbits/make-testlab-client
```

<Warning>
  Do **not** use the retired name `make-testlab-playwright-reporter`.
</Warning>

## Path

1. [CLI login + init](/cli-sdk/cli-login-init)
2. Import catalog in the [Cases UI](/web-app/cases)
3. [Playwright reporter](/cli-sdk/playwright) for day-to-day runs
4. Or call the [Client](/cli-sdk/client) from a non-Playwright runner

## What the Playwright path covers

| Concern                    | Where documented                                                                                      |
| -------------------------- | ----------------------------------------------------------------------------------------------------- |
| Send results               | [Playwright SDK → How results are sent](/cli-sdk/playwright#how-results-are-sent)                |
| Attach screenshots         | [Playwright SDK → Screenshots and videos](/cli-sdk/playwright#screenshots-and-videos)            |
| Test steps / Observed      | [Playwright SDK → Test steps](/cli-sdk/playwright#test-steps-observed-evidence)                  |
| Evidence policy            | [Evidence](/concepts/evidence)                                                                   |
| Env vars                   | [Reporting env](/concepts/reporting-env)                                                         |
| Catalog / suite membership | [Portable Catalog](/concepts/portable-catalog)                                                   |
| Non-Playwright runners     | [Client → Non-Playwright](/cli-sdk/client#non-playwright-runners-maestro-junit-custom)           |
| CI/CD                      | [GitHub Actions](/ci-automations/github-actions) + [CI/CD guide](/guides/ci-cd-integration) |
| Flutter web sample         | [ovenbits/flutter-web-playwright](https://github.com/ovenbits/flutter-web-playwright)                 |
